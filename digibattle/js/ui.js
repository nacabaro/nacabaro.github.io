class VPetInfo {
    constructor() {
        this.device = NaN;
        this.code = NaN;
    }

    setDevice(device) { self.device = device; }
    getDevice() { return self.device; }

    setCode(code) { self.code = code; }
    getCode() { return self.code; }
}